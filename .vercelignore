# Runtime state that lives beside the code on the worker machine and
# must never ride along to a deploy. The browser profile in particular
# contains Chrome's SingletonLock, a symlink broken anywhere but here,
# which fails Vercel's upload tracing with ENOENT.
.browser-profile
.secrets
logs
.next
.fill-runs
.workday-auth
.claude
